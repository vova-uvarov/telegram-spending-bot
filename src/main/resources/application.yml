spring:
  datasource:
    url: jdbc:h2:mem:spending-bot
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
management:
  endpoints:
    web:
      exposure:
        include: "*"
app:
  telegram:
    bot:
      name: SpendingBot
      token:
      proxyHost:
      proxyPort:
      proxyType:
      proxyUser:
      proxyPassword:
